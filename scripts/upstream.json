{
  "$comment": [
    "Audited external versions cited in shipped guidance, and where each is cited.",
    "Two checks run against this file. Coherence (offline, part of `make check`):",
    "every audited version must still appear in each doc that cites it, so the",
    "manifest cannot drift away from the prose. Freshness (network, nightly):",
    "the audited version is compared against the current upstream release.",
    "",
    "`sources` are all machine-readable and readable unauthenticated.",
    "API endpoint paths are deliberately NOT tracked here: HCP Terraform publishes",
    "no machine-readable API schema to diff them against, so any check would be a",
    "hand-maintained second list of the same strings."
  ],
  "entries": [
    {
      "name": "cf-terraforming",
      "source": {"type": "github_release", "repo": "cloudflare/cf-terraforming"},
      "audited": "0.27.0",
      "audited_date": "2026-05",
      "cited_in": ["docs/import.md"],
      "why_it_matters": "The per-resource coverage matrix in import.md was written against this release; newer ones may cover more or differ in generated HCL."
    },
    {
      "name": "terraform",
      "source": {"type": "github_release", "repo": "hashicorp/terraform"},
      "audited": "1.15.9",
      "cited_in": ["docs/setup.md"],
      "why_it_matters": "Appears in the worked mise.toml a reader copies to establish the toolchain pin."
    },
    {
      "name": "gh",
      "source": {"type": "github_release", "repo": "cli/cli"},
      "audited": "2.81.0",
      "cited_in": ["docs/setup.md"],
      "why_it_matters": "Same worked mise.toml."
    },
    {
      "name": "jq",
      "source": {"type": "github_release", "repo": "jqlang/jq"},
      "audited": "1.8.1",
      "cited_in": ["docs/setup.md"],
      "why_it_matters": "Same worked mise.toml."
    },
    {
      "name": "mise-action",
      "source": {"type": "github_release", "repo": "jdx/mise-action"},
      "audited": "4.3.0",
      "cited_in": ["docs/ci.md"],
      "why_it_matters": "Pinned by commit SHA in the CI example, with this version as the human-readable comment."
    },
    {
      "name": "cloudflare-provider-major",
      "source": {"type": "terraform_provider", "namespace": "cloudflare", "name": "cloudflare"},
      "audited": "5",
      "compare": "major",
      "cited_in": ["cloudflare.md", "docs/import.md"],
      "why_it_matters": "Guidance states behaviour specific to the v5 provider; a new major invalidates it."
    }
  ]
}
