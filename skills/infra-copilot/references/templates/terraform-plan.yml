# Terraform Plan workflow for object-storage backend mode
# Copy to .github/workflows/terraform-plan.yml and customize for your setup
name: Terraform Plan

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  id-token: write

env:
  TF_IN_AUTOMATION: true
  TF_INPUT: false

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      cloudflare: ${{ steps.filter.outputs.cloudflare }}
      github: ${{ steps.filter.outputs.github }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - uses: dorny/paths-filter@0e4a8c6effa4802afeda77dc8d303f8176d7dfad # v3
        id: filter
        with:
          filters: |
            cloudflare:
              - 'terraform/cloudflare/**'
              - 'terraform/modules/**'
              - 'mise.toml'
              - '.infra-copilot/config.md'
            github:
              - 'terraform/github/**'
              - 'terraform/modules/**'
              - 'mise.toml'
              - '.infra-copilot/config.md'

  plan-cloudflare:
    needs: changes
    if: needs.changes.outputs.cloudflare == 'true' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4

      # Authenticate to cloud provider for state access
      # Uncomment and configure for your backend:
      #
      # GCS:
      # - uses: google-github-actions/auth@c200f3691d83b41bf9bbd8638997a462592937ed # v2
      #   with:
      #     workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
      #     service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
      #
      # S3:
      # - uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a # v4
      #   with:
      #     role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
      #     aws-region: us-east-1
      #
      # Azure:
      # - uses: azure/login@7184910d9eb2b1c5e48f7073824a90609bb9b6d6 # v2
      #   with:
      #     client-id: ${{ secrets.AZURE_CLIENT_ID }}
      #     tenant-id: ${{ secrets.AZURE_TENANT_ID }}
      #     subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Terraform Init
        working-directory: terraform/cloudflare
        run: terraform init

      - name: Terraform Plan
        id: plan
        working-directory: terraform/cloudflare
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          TF_VAR_cloudflare_api_token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          terraform plan -no-color -out=tfplan 2>&1 | tee plan.txt
          echo "exitcode=${PIPESTATUS[0]}" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: Post Plan to PR
        uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b # v7
        if: github.event_name == 'pull_request'
        with:
          script: |
            const fs = require('fs');
            const plan = fs.readFileSync('terraform/cloudflare/plan.txt', 'utf8');
            const maxLength = 65000;
            const truncated = plan.length > maxLength
              ? plan.substring(0, maxLength) + '\n\n... (truncated)'
              : plan;

            const body = `### Terraform Plan: \`cloudflare\`

            <details>
            <summary>Show Plan</summary>

            \`\`\`
            ${truncated}
            \`\`\`
            </details>`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });

      - name: Check Plan Status
        if: steps.plan.outputs.exitcode != '0'
        run: exit 1

  plan-github:
    needs: changes
    if: needs.changes.outputs.github == 'true' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4

      # Authenticate to cloud provider for state access (same as above)

      - name: Terraform Init
        working-directory: terraform/github
        run: terraform init

      - name: Terraform Plan
        id: plan
        working-directory: terraform/github
        env:
          GITHUB_APP_ID: ${{ secrets.GH_APP_ID }}
          GITHUB_APP_INSTALLATION_ID: ${{ secrets.GH_APP_INSTALLATION_ID }}
          GITHUB_APP_PEM_FILE: ${{ secrets.GH_APP_PEM }}
          TF_VAR_github_app_id: ${{ secrets.GH_APP_ID }}
          TF_VAR_github_app_installation_id: ${{ secrets.GH_APP_INSTALLATION_ID }}
          TF_VAR_github_app_pem: ${{ secrets.GH_APP_PEM }}
          TF_VAR_github_owner: ${{ vars.GITHUB_ORG || github.repository_owner }}
        run: |
          terraform plan -no-color -out=tfplan 2>&1 | tee plan.txt
          echo "exitcode=${PIPESTATUS[0]}" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: Post Plan to PR
        uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b # v7
        if: github.event_name == 'pull_request'
        with:
          script: |
            const fs = require('fs');
            const plan = fs.readFileSync('terraform/github/plan.txt', 'utf8');
            const maxLength = 65000;
            const truncated = plan.length > maxLength
              ? plan.substring(0, maxLength) + '\n\n... (truncated)'
              : plan;

            const body = `### Terraform Plan: \`github\`

            <details>
            <summary>Show Plan</summary>

            \`\`\`
            ${truncated}
            \`\`\`
            </details>`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });

      - name: Check Plan Status
        if: steps.plan.outputs.exitcode != '0'
        run: exit 1

  # Validation jobs run for all PRs including forks (no secrets needed)
  validate:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        leaf: [cloudflare, github]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4

      - name: Terraform Format
        run: terraform fmt -check -recursive terraform/

      - name: Terraform Init (no backend)
        working-directory: terraform/${{ matrix.leaf }}
        run: terraform init -backend=false

      - name: Terraform Validate
        working-directory: terraform/${{ matrix.leaf }}
        run: terraform validate

  # Aggregate status check for branch protection
  plan:
    needs: [changes, plan-cloudflare, plan-github, validate]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Evaluate prerequisite job statuses
        env:
          CLOUDFLARE_CHANGED: ${{ needs.changes.outputs.cloudflare }}
          GITHUB_CHANGED: ${{ needs.changes.outputs.github }}
        run: |
          # Check for failures and cancellations
          failures=$(echo '${{ toJson(needs) }}' | jq -r 'to_entries[] | select(.value.result == "failure" or .value.result == "cancelled") | .key')
          if [ -n "$failures" ]; then
            echo "Prerequisite job(s) failed or cancelled: $failures"
            exit 1
          fi
          # Ensure changed leaves have successful (not skipped) plans
          # A skipped plan for a changed leaf means no one reviewed the changes
          plan_cloudflare_result=$(echo '${{ toJson(needs) }}' | jq -r '.["plan-cloudflare"].result // "skipped"')
          plan_github_result=$(echo '${{ toJson(needs) }}' | jq -r '.["plan-github"].result // "skipped"')
          if [ "$CLOUDFLARE_CHANGED" = "true" ] && [ "$plan_cloudflare_result" != "success" ]; then
            echo "Error: terraform/cloudflare changed but plan-cloudflare did not succeed (result: $plan_cloudflare_result)"
            echo "This may be a fork PR - fork PRs cannot run authenticated plans."
            exit 1
          fi
          if [ "$GITHUB_CHANGED" = "true" ] && [ "$plan_github_result" != "success" ]; then
            echo "Error: terraform/github changed but plan-github did not succeed (result: $plan_github_result)"
            echo "This may be a fork PR - fork PRs cannot run authenticated plans."
            exit 1
          fi
          echo "All plan and validation checks passed."
